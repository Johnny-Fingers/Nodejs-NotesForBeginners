// Node.js use CommonJS by default, for that reason, the posibility of see code with this module system exists. It looks like this:
const sum = (a, b) => a + b;

module.exports = { sum };

// However, we have to consider that nowadays the most used schema is ES Module.